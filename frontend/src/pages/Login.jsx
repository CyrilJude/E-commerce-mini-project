import {useState}from'react';import{Link,useLocation,useNavigate}from'react-router-dom';import api from'../services/api';import{useAuth}from'../context/AuthContext';export default function Login(){const[f,setF]=useState({email:'',password:''}),[e,setE]=useState(''),{save}=useAuth(),nav=useNavigate(),loc=useLocation();async function submit(x){x.preventDefault();try{save((await api.post('/auth/login',f)).data);nav(loc.state?.from||'/')}catch(x){setE(x.response?.data?.message||'Login failed')}}return <AuthForm title="Welcome back" subtitle="Sign in to continue" fields={f} setFields={setF} onSubmit={submit} error={e} button="Login" footer={<>New here? <Link to="/register">Create an account</Link></>}/>}export function AuthForm({title,subtitle,fields,setFields,onSubmit,error,button,footer,register}){return <main className="auth"><form className="authBox" onSubmit={onSubmit}><p className="eyebrow">SHOPNEST</p><h1>{title}</h1><p>{subtitle}</p>{register&&<input required placeholder="Full name" value={fields.name} onChange={e=>setFields({...fields,name:e.target.value})}/>}<input required type="email" placeholder="Email" value={fields.email} onChange={e=>setFields({...fields,email:e.target.value})}/><input required minLength="6" type="password" placeholder="Password" value={fields.password} onChange={e=>setFields({...fields,password:e.target.value})}/>{register&&<><input placeholder="Phone" value={fields.phone} onChange={e=>setFields({...fields,phone:e.target.value})}/><textarea placeholder="Address" value={fields.address} onChange={e=>setFields({...fields,address:e.target.value})}/></>}{error&&<div className="error">{error}</div>}<button className="primary">{button}</button><div>{footer}</div></form></main>}
