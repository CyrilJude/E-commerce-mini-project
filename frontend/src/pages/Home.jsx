import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import api from '../services/api';import {useAuth} from '../context/AuthContext';export default function Home(){const[p,setP]=useState([]),[q,setQ]=useState('');const{user}=useAuth();useEffect(()=>{api.get('/products').then(r=>setP(r.data))},[]);const list=p.filter(x=>(x.name+' '+x.category).toLowerCase().includes(q.toLowerCase()));async function add(x){if(!user)return alert('Please login to add items to your cart.');await api.post('/cart/items',{productId:x.id,quantity:1});alert('Added to cart');}return <main><section className="hero"><p className="eyebrow">FULL-STACK E-COMMERCE</p><h1>Everything you need, in one place.</h1><p>Browse products, manage your cart and place orders through a modern React + Spring Boot application.</p></section><div className="toolbar"><h2>Products</h2><input placeholder="Search products..." value={q} onChange={e=>setQ(e.target.value)}/></div><section className="grid">{list.map(x=><article className="card" key={x.id}><img src={x.imageUrl}/><div className="cardBody"><span>{x.category}</span><h3>{x.name}</h3><p>{x.description}</p><strong>₹{x.price.toLocaleString('en-IN')}</strong><div className="actions"><Link to={'/products/'+x.id}>View</Link><button onClick={()=>add(x)}>Add to cart</button></div></div></article>)}</section></main>}
