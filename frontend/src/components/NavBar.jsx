import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';export default function NavBar(){const {user,logout}=useAuth(),nav=useNavigate();return <header><Link className="brand" to="/">ShopNest</Link><nav><Link to="/">Products</Link>{user&&<><Link to="/orders">My Orders</Link><Link to="/profile">Profile</Link><button className="linkBtn" onClick={()=>{logout();nav('/login')}}>Logout</button></>}{!user&&<Link to="/login">Login</Link>}<Link className="cartLink" to="/cart">Cart</Link></nav></header>}
